import React from 'react';
import ReactDOM from 'react-dom/client';
import VideoToolApp from './VideoToolApp';

ReactDOM.createRoot( document.getElementById( 'root' )! ).render(
  <React.StrictMode>
    <VideoToolApp />
  </React.StrictMode>,
);
